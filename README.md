# Cool-Milsim-Website-Concept

Static milsim-themed landing concept site — "Lockheed Martin // Advanced
Intel-Dir Vector Matrix" style hero with multi-domain dominance messaging.

Plain HTML/CSS/JS, no build step. CI (`.github/workflows/ci.yml`) validates
the HTML with `tidy` and checks internal links/anchors with `lychee`.

Open `index.html` in a browser to view.
